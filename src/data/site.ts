/** Path inside the public bucket (plus optional VITE_SUPABASE_STORAGE_PREFIX). */
export const aboutPhotoStoragePath = "ui/about-me/01.jpg";

/** If Storage is unreachable or the object is missing. */
export const aboutPhotoFallbackSrc = "/fallback.png";
